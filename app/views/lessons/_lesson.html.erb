<div class="lessons-content">
	<%= lesson.title %>
	<%= lesson.description %>
	<%= link_to 'Show', [lesson.lecture, lesson] %>
	<% if current_user ==  @lecture.user  %>
	<%= link_to 'Delete', [lesson.lecture, lesson], method: :delete %>
	<% end %>
</div>