<% content_for :navbar do %>
	<%= render 'layouts/sidebar' %>
<% end %>

<% content_for :head do %>
 <!-- Start of workshoprsupport Zendesk Widget script -->
<script>

$(document).on('page:change', function () {
  // Actions to do
 window.zEmbed||function(e,t){var n,o,d,i,s,a=[],r=document.createElement("iframe");window.zEmbed=function(){a.push(arguments)},window.zE=window.zE||window.zEmbed,r.src="javascript:false",r.title="",r.role="presentation",(r.frameElement||r).style.cssText="display: none",d=document.getElementsByTagName("script"),d=d[d.length-1],d.parentNode.insertBefore(r,d),i=r.contentWindow,s=i.document;try{o=s}catch(c){n=document.domain,r.src='javascript:var d=document.open();d.domain="'+n+'";void(0);',o=s}o.open()._l=function(){var o=this.createElement("script");n&&(this.domain=n),o.id="js-iframe-async",o.src=e,this.t=+new Date,this.zendeskHost=t,this.zEQueue=a,this.body.appendChild(o)},o.write('<body onload="document._l();">'),o.close()}("https://assets.zendesk.com/embeddable_framework/main.js","workshoprsupport.zendesk.com");
 
});


</script>
<!-- End of workshoprsupport Zendesk Widget script -->
<% end %>



<div class="row" id="dashboard-header" data-no-turbolink>
	<h1 class="dashboard-title">enrolled courses</h1>
	<% if !@user.lectures.exists? %>
		<%= link_to lectures_path, class: "btn btn-bezier btn-small" do %>
				Join a course!
		<% end %> 
	<% end %>	
</div>




<div class="row" id="enrolled-courses">



		<% @user.lectures.each do |lecture| %>
		  <div class="col-md-4">
	         <div class="dashboard-student-box">
				<%= image_tag(lecture.picture.url) %>
				<%= link_to lecture do %>	
		        <h3 class="center"><%= lecture.title %></h3> 
		        <% end %> 
		        <p class="center by-teacher">by <%= lecture.teacher.name %></p>
	           
				<div class="section-box-bottom">
		           <progress max="<%= lecture.lessons.size %>" value="<%= lecture.lessons.select{ |l| l.users.include? current_user }.count  %>">
		           </progress>
		            <p class="completed-lessons">
		            <%= lecture.lessons.select{ |l| l.users.include? current_user }.count %> /  <%= lecture.lessons.size %> lessons completed
		            </p>
		         
		        </div>   

	        </div>
	      </div> <!-- .lecture-box -->
        <% end %>


</div>	




